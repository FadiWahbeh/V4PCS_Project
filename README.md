# V4PCS_Project

Description: 3D point cloud voxelization and planar patch extraction.

Build (Windows):

1. mkdir build
2. cd build
3. cmake -G "Ninja" ..
4. cmake --build .

Usage:

Executable reads files from `data/source` and writes to `data/output`.

Licence: Add your licence here.
